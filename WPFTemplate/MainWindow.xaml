<Window x:Class="WPFTemplate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTemplate"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen"
        xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Window.Resources>

        <x:Array x:Key="strs" Type="{x:Type sys:String}">
            <sys:String>Alerts</sys:String>
            <sys:String>Badges</sys:String>
            <sys:String>Buttons</sys:String>
            <sys:String>Cards</sys:String>
            <sys:String>Forms</sys:String>
            <sys:String>Modals</sys:String>
        </x:Array>

        <x:Array x:Key="peoples" Type="{x:Type local:People}">
            <local:People Name="Alerts" Age="1"/>
            <local:People Name="Badges" Age="2"/>
            <local:People Name="Buttons" Age="3"/>
            <local:People Name="Cards" Age="4"/>
            <local:People Name="Forms" Age="5"/>
            <local:People Name="Modals" Age="6"/>
        </x:Array>

        <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Height" Value="10"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border x:Name="PART_Border">
                            <TextBlock Text="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="PART_Border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                    
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="slider1" TargetType="{x:Type Slider}">
            <Setter Property="Height" Value="20"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition MaxWidth="10"/>
                            </Grid.ColumnDefinitions>
                            <local:CornerTextBox x:Name="PART_TextBox" Text="{Binding RelativeSource={RelativeSource AncestorType=Slider}, Path=Value}" 
                                                 VerticalContentAlignment="Center" Grid.ColumnSpan="2" CornerRadius="4"
                                                 BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"/>
                            <StackPanel Grid.Column="1" Orientation="Vertical">
                                <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Content="+" Style="{StaticResource RepeatButtonTransparent}"/>
                                <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Content="-"  Style="{StaticResource RepeatButtonTransparent}"/>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </Window.Resources>
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <local:CornerSlider x:Name="slider1" Width="100" VerticalAlignment="Center" 
                            Maximum="100" Minimum="-100" TickFrequency="0.1"
                            Value="0" IsSnapToTickEnabled="True" BorderBrush="Red" BorderThickness="1"/>
        <TextBox Text="{Binding ElementName=slider1, Path=Value, Mode=TwoWay}" Width="100"/>
        <local:CornerTextBox Width="100" Margin="0,10"/>
        <Slider LargeChange="0.1" BorderBrush="Green" Style="{StaticResource slider1}"/>
        <local:CornerSlider Width="100" Margin="0,10" SliderType="Box" CornerRadius="5" BorderBrush="LightBlue"/>
    </StackPanel>
</Window>
