<Window x:Class="WPFTemplate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTemplate"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen"
        xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Window.Resources>
        
        <x:Array x:Key="strs" Type="{x:Type sys:String}">
            <sys:String>Alerts</sys:String>
            <sys:String>Badges</sys:String>
            <sys:String>Buttons</sys:String>
            <sys:String>Cards</sys:String>
            <sys:String>Forms</sys:String>
            <sys:String>Modals</sys:String>
        </x:Array>

        <x:Array x:Key="peoples" Type="{x:Type local:People}">
            <local:People Name="Alerts" Age="1"/>
            <local:People Name="Badges" Age="2"/>
            <local:People Name="Buttons" Age="3"/>
            <local:People Name="Cards" Age="4"/>
            <local:People Name="Forms" Age="5"/>
            <local:People Name="Modals" Age="6"/>
        </x:Array>

        <FontFamily x:Key="FatSheep">pack://application:,,,/WPFTemplate;component/Resources/iconfont.ttf#FatSheep</FontFamily>

        <Style x:Key="MenuItemButtonStyle1" TargetType="{x:Type local:CornerButton}" BasedOn="{StaticResource {x:Type local:CornerButton}}">
            <Setter Property="Width" Value="150"/>
            <Setter Property="ButtonStyle" Value="Normal"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="MenuItemStyle1" TargetType="{x:Type local:CornerMenuItem}" BasedOn="{StaticResource {x:Type local:CornerMenuItem}}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="150"/>
        </Style>
    </Window.Resources>
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="ContextMenu">
            <TextBlock.ContextMenu>
                <local:CornerContextMenu>
                    <local:CornerMenuItem Header="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Role}" 
                                          VerticalAlignment="Center" Height="40" Icon="&#xe690;"/>
                    <local:CornerMenuItem Header="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Role}" 
                                          VerticalAlignment="Center" Height="40" Icon="&#xe690;"/>
                    <local:CornerMenuItem Header="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Role}" 
                                          VerticalAlignment="Center" Height="40" Icon="&#xe690;"/>
                    <local:CornerMenuItem Header="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Role}" 
                                          VerticalAlignment="Center" Height="40" Icon="&#xe690;"/>
                </local:CornerContextMenu>
            </TextBlock.ContextMenu>
        </TextBlock>
    </StackPanel>
</Window>
