<UserControl x:Class="WpfBootstrap.View.LoadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfBootstrap.View"
             xmlns:cc="clr-namespace:WPFTemplate;assembly=WPFTemplate"
             mc:Ignorable="d" 
             Background="Black"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <cc:Loading LoadingType="{Binding ElementName=cbType, Path=Text}"/>
        <cc:CornerComboBox x:Name="cbType" Width="200" Height="30" SelectedIndex="0" Margin="0,300,0,0" VerticalContentAlignment="Center">
            <ComboBoxItem Content="Type1"/>
            <ComboBoxItem Content="Type2"/>
        </cc:CornerComboBox>
    </Grid>
</UserControl>
